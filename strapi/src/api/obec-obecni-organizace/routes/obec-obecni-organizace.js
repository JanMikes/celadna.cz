'use strict';

/**
 * obec-obecni-organizace router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::obec-obecni-organizace.obec-obecni-organizace');
