'use strict';

/**
 *  obec-obecni-organizace controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::obec-obecni-organizace.obec-obecni-organizace');
