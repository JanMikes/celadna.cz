'use strict';

/**
 * obec-obecni-organizace service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::obec-obecni-organizace.obec-obecni-organizace');
