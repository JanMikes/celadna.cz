{% extends 'base.html.twig' %}

{% block title %}Prohlášení o přístupnosti{% endblock %}

{% block content %}
    {{ include('_breadcrumb.html.twig', {
        'current': block('title'),
        'items': {},
    }) }}

    <article class="simple-text-section" role="region" aria-label="{{ pristupnost.Nadpis }}">
        <h2>{{ pristupnost.Nadpis }}</h2>

        {{ pristupnost.Obsah|markdown_to_html|nl2br }}
    </article>
{% endblock %}
